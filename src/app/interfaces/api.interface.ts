export type InvidiousData = [
  string,
  {
    cors: boolean;
    api: boolean;
    type: string;
    uri: string;
  },
];

export type Instances = {
  cors: boolean;
  api: boolean;
  type: string;
  uri: string;
};